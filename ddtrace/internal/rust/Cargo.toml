[package]
name = "ddtrace"
version = "1.0.0"
edition = "2018"

[lib]
name = "ddtrace"
# "cdylib" is necessary to produce a shared library for Python to import from.
crate-type = ["cdylib"]

[dependencies.pyo3]
version = "0.13.2"
features = ["extension-module"]